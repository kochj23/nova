[pytest]
testpaths = tests
import_mode = importlib
markers =
    integration: tests that hit real local services
    functional: end-to-end workflow tests
    frame: browser/UI frame tests
    dashboard: dashboard server unit tests
